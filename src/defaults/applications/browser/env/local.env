# The demo site runs on this machine, on a port the OS picks: there is
# nothing to configure. The file exists because every application needs one
# environment file per environment it runs in.
DEMO=local
